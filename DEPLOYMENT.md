Create three Vercel projects pointing to this repo:

- Admin: root directory `apps/admin`
- Webapp: root directory `apps/webapp`
- API: root directory `apps/api`

Each app uses its local `vercel.json` for build settings.

